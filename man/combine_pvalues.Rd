% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_pvalues.R
\name{combine_pvalues}
\alias{combine_pvalues}
\title{Combine per-tool p-values into one significance call per taxon}
\usage{
combine_pvalues(
  consolidated,
  method = c("fisher", "stouffer", "cauchy"),
  weights = NULL,
  fdr_cut = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{consolidated}{Output of consolidate_DA_results() — needs `pval_<tool>`.}

\item{method}{One of "fisher", "stouffer", "cauchy".}

\item{weights}{Named numeric vector of per-tool weights, Stouffer only.}

\item{fdr_cut}{Numeric, BH threshold on the combined p-value.}

\item{verbose}{Logical, print the significance count message.}
}
\value{
`consolidated` + `p_combined`, `padj_combined`, `sig_combined`.
}
\description{
One pass over `pval_<tool>` columns — no rerunning tools, unlike
estimate_fdr_by_permutation(). Fisher/Stouffer require the tools'
p-values to be independent under the null; this dataset's tools are
documented to be correlated (e.g. co-firing false positives), which makes
Fisher/Stouffer anti-conservative. Prefer `method = "cauchy"`, which is
valid under arbitrary dependence (Liu & Xie 2020) and is the only method
here with an actual theoretical FDR guarantee under dependence.
}
