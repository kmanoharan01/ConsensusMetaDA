% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus_vote.R
\name{estimate_fdr_by_permutation}
\alias{estimate_fdr_by_permutation}
\title{Empirical FDR of vote rules via permutation}
\usage{
estimate_fdr_by_permutation(
  physeq,
  group_var = "Group",
  covariate = NULL,
  group_order = NULL,
  fdr_cut = 0.05,
  tools = c("edgeR", "DESeq2", "ALDEx2", "metaSeq", "ADAPT", "ANCOMBC2", "MaAsLin3"),
  cores = 1L,
  n_perm = 10L,
  seed = 1L,
  outdir = tempdir()
)
}
\arguments{
\item{physeq, group_var, covariate, group_order, fdr_cut, tools, cores}{As in
OTUs_multi_DA().}

\item{n_perm}{Integer, number of permutations.}

\item{seed}{Integer.}

\item{outdir}{As in OTUs_multi_DA(); permutation runs use subdirectories
and skip plots.}
}
\value{
data.frame, one row per vote rule per contrast.
}
\description{
Estimates each vote rule's false discovery proportion for THIS dataset by
permuting group labels (null true by construction), rerunning the full
pipeline, and comparing null call counts to the observed. Expensive:
reruns all `tools` x `n_perm` times.
}
