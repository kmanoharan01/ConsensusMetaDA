% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_pvalues.R
\name{add_combined_pvalues}
\alias{add_combined_pvalues}
\title{Append several p-value combination methods at once}
\usage{
add_combined_pvalues(
  consolidated,
  methods = c("fisher", "stouffer", "cauchy"),
  weights = NULL,
  fdr_cut = 0.05,
  verbose = FALSE
)
}
\arguments{
\item{consolidated}{Output of consolidate_DA_results() — needs `pval_<tool>`.}

\item{methods}{Character vector, subset of c("fisher","stouffer","cauchy").}

\item{weights}{Named numeric vector of per-tool weights (Stouffer only).}

\item{fdr_cut}{Numeric, BH threshold on each combined p-value.}

\item{verbose}{Logical.}
}
\value{
`consolidated` with `p_<method>`, `padj_<method>`, `sig_<method>`
  appended for each requested method.
}
\description{
Like [combine_pvalues()] but writes method-suffixed columns
(`p_<method>`, `padj_<method>`, `sig_<method>`) for every method in
`methods`, so a single consolidated table can carry Fisher, Stouffer, and
Cauchy side by side. This is what [consolidate_DA_results()] calls when
`add_combined = TRUE`, and what puts the combined columns into the
`*_ALLDE_results.txt` file.
}
