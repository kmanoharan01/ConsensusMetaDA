% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OTUs_multi_DA.R
\name{OTUs_multi_DA}
\alias{OTUs_multi_DA}
\title{Multi-method differential abundance with consensus}
\usage{
OTUs_multi_DA(
  physeq,
  group_var = "Group",
  covariate = NULL,
  group_order = NULL,
  fdr_cut = 0.05,
  outdir = tempdir(),
  tools = c("edgeR", "DESeq2", "ALDEx2", "metaSeq", "ADAPT", "ANCOMBC2", "MaAsLin3"),
  cores = 1L,
  make_plots = TRUE,
  add_combined = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{physeq}{A phyloseq object.}

\item{group_var}{Character. Metadata column holding the grouping variable.}

\item{covariate}{Character or NULL. Optional second metadata column to
adjust for; model becomes `~ group_var + covariate` and edgeR switches
from exactTest to a quasi-likelihood GLM.}

\item{group_order}{Character vector giving level order; first element of
each pair is the reference.}

\item{fdr_cut}{Numeric. Adjusted p-value threshold defining significance.}

\item{outdir}{Directory for result files. Defaults to tempdir().}

\item{tools}{Character vector of tools to run.}

\item{cores}{Integer, cores for ALDEx2's Monte Carlo sampling.}

\item{make_plots}{Logical, write an UpSet diagram per contrast.}

\item{add_combined}{Logical, append Fisher/Stouffer/Cauchy combined p-values
to the consolidated table (and the ALLDE file). Default TRUE.}

\item{verbose}{Logical.}
}
\value{
Invisibly, a named list with one element per contrast, each
  holding the per-tool raw results and `$consolidated`.
}
\description{
Runs up to seven DA tools on each pairwise contrast of a grouping variable
and consolidates the results into one table carrying per-taxon agreement
counts (`n_sig`, `n_tools`) and, when `add_combined = TRUE`, the
Fisher/Stouffer/Cauchy combined p-values.
}
