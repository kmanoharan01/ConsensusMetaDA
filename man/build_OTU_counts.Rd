% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_OTU_counts.R
\name{build_OTU_counts}
\alias{build_OTU_counts}
\title{Build and filter a phyloseq object from microbiome data}
\usage{
build_OTU_counts(
  physeq = NULL,
  biom = NULL,
  sample_table = NULL,
  tax_tables = NULL,
  taxa_level = NULL,
  include_taxonomy = TRUE,
  abundance_threshold = NULL,
  prevalence_threshold = NULL,
  rarity_threshold = NULL,
  variance_threshold = NULL,
  force_build = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{physeq}{A prebuilt phyloseq object. If supplied, `biom` /
`sample_table` / `tax_tables` are ignored and the object goes straight
into the filtering cascade. Default NULL.}

\item{biom}{Character path to a BIOM file (JSON or HDF5). Used only when
`physeq` is NULL. Default NULL.}

\item{sample_table}{Character path to a QIIME-format sample metadata file.
Used only when `physeq` is NULL. Default NULL.}

\item{tax_tables}{Optional taxonomy table (path or object accepted by
phyloseq::tax_table). Used only when `physeq` is NULL. Default NULL.}

\item{taxa_level}{Character taxonomic rank to agglomerate to (e.g. "Genus"),
or NULL for no agglomeration.}

\item{include_taxonomy}{Logical, whether to attach taxonomy when importing
from files. Default TRUE.}

\item{abundance_threshold}{Numeric, minimum total abundance to retain an
OTU, or NULL.}

\item{prevalence_threshold}{Numeric in (0,1), minimum fraction of samples an
OTU must appear in, or NULL.}

\item{rarity_threshold}{Numeric, remove OTUs with total abundance below this,
or NULL.}

\item{variance_threshold}{Numeric, minimum across-sample variance to retain,
or NULL.}

\item{force_build}{Logical, currently unused. Default FALSE.}

\item{verbose}{Logical, print filtering progress. Default FALSE.}
}
\value{
A filtered phyloseq object.
}
\description{
Two entry points in v2:
\enumerate{
  \item Pass an already-built phyloseq object via `physeq =` and use this
    function purely for the filtering cascade (abundance / prevalence /
    rarity / variance / agglomeration).
  \item Pass file paths via `biom = ` and `sample_table = ` (plus optional
    `tax_tables = `) to import from disk and then filter.
}
Exactly one of `physeq` or (`biom` + `sample_table`) must be supplied.
}
